# Quarantine Intake Patch

Prepared because the GitHub integration returned 403 on the transaction manifest. Nothing in this directory should be treated as promoted runtime canon until the repository intake/audit gates pass.
