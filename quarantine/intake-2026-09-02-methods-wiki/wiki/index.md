# Derived ToolsForCodex Knowledge Map Prototype

This is a router, not canon.

- Repo writes / external intake → `AGENTS.md` + `docs/adding-repo-protocol.md`
- MCP skill discovery → `src/mcp/server.ts` + `skills/`
- Durable project state → Notion canonical ledger; repo `context.md` is batch-only
- Release validation → `package.json`, `scripts/validate-release.mjs`, CI
- Method routing → BMAD (planning depth), Fable (proof), Entrepreneur OS (venture leverage/experiments)

Never copy mutable state into this wiki.
