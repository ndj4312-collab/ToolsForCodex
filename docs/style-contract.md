# Style contract

The package uses Markdown for human-facing instructions and JSON for machine-facing interchange. YAML is limited to runtime metadata. Canonical files use UTF-8, LF line endings, kebab-case skill names, explicit frontmatter, and stable relative POSIX paths. Generated views may be regenerated; they never become editable canon.
