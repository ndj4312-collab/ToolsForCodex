# Skill index

This is the generated-facing index for the project-local orchestration package. Canonical instructions remain in each skill's `SKILL.md`.

- `/production-orchestrator` — the user-invoked router and transaction controller.
- `/ingest-and-endure` — deterministic exhaustive corpus assimilation with per-item accounting, routing, implementation-gap handling, acceptance, endurance, and coverage gates.
- `/headroom-token-minimizer` — a model-invoked context and tool-output minimizer that preserves audit evidence.
- `/standardize-skills-to-matt-pocock` — the user-invoked normalization controller.
- `/matt-skill-contract-audit` — the model-invoked metadata and compatibility validator.
- `skill-context-builder` — persistent current-canon context compiler for orchestration surfaces, dependencies, worker kernels, and execution-state reconciliation.
- `/agentic-intel` — evidence-governed external intelligence research, primitive decomposition, convergence, and agentic-system application.
- `/dispel` — compile a frozen ideal target into the smallest dependency-closed canonical ecosystem before specification or implementation.
- `project-planning-compiler` — select project-planning depth, resolve only missing prerequisites, and compose the exact vendored Matt Pocock `/to-spec` and `/to-tickets` skills into dependency-safe executable work without modifying either upstream skill.
- `/traverse` — deterministic persistent realization runtime: freeze an immutable desired end state, repeatedly reconcile and execute bounded dependency-safe parallel frontiers, require full regression before every frontier closes, route earliest-cause repairs through the correct owners, preserve cumulative recovery/HOLD state, and terminate only on exact success or a defined handoff.
